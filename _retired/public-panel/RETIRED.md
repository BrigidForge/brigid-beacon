# RETIRED — public-panel

This was a standalone public panel skeleton. It is retired and superseded.

**Replaced by:** `apps/vault-ui` — React Router routes `/view` and `/view/:vault`

Do not modify, reference, or build from this directory.
